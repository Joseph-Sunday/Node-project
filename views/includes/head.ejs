<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title><%=  pageTitle %></title>
        <link rel="stylesheet" href="/css/home.css">
        <% if(path === '/cart') { %>
            <link rel="stylesheet" href="/css/cart.css">
        <% } %>
        <% if(path === '/add-product' || path === '/login') { %>
            <link rel="stylesheet" href="/css/add-product.css">
        <% } %>
    </head>
    <body>
        <header>
            <nav class="header__nav">
                <ul class="header-nav__ul">
                    <li class="header-nav-ul__list">
                        <a href="/" class="<% if (path === '/home') { %>active<% } else { %>zero<% } %>">Home</a>
                    </li>
                    <li class="header-nav-ul__list">
                        <a href="/products" class="<% if (path === '/product') { %>active<% } else { %>zero<% } %>">Products</a>
                    </li>
                    <% if(isAuthenticated) { %>
                        <li class="header-nav-ul__list">
                            <a href="/cart" class="<% if (path === '/cart') { %>active<% } else { %>zero<% } %>">Cart</a>
                        </li>
                        <li class="header-nav-ul__list">
                            <a href="/orders" class="<% if (path === '/orders') { %>active<% } else { %>zero<% } %>">Orders</a>
                        </li>
                        <li class="header-nav-ul__list">
                            <a href="/add-product" class="<% if (path === '/add-product') { %>active<% } else { %>zero<% } %>">Add Product</a>
                        </li>
                        <li class="header-nav-ul__list">
                            <a href="/admin-products" class="<% if (path === '/admin-products') { %>active<% } else { %>zero<% } %>">Admin products</a>
                        </li>
                    <% } %>
                </ul>
                <ul class="header-nav__ul">
                    <% if(!isAuthenticated) { %>
                        <li class="header-nav-ul__list">
                            <a href="/login" class="<% if (path === '/login') { %>active<% } else { %>zero<% } %>">
                                Login
                            </a>
                        </li>
                        <li class="header-nav-ul__list">
                            <a href="/signup" class="<% if (path === '/signup') { %>active<% } else { %>zero<% } %>">
                                Signup
                            </a>
                        </li>
                    <% } else { %>
                        <li class=" header-nav-ul__list">
                            <form action="/logout" method="POST">
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                    <button type="submit" class="logout"> Logout </button>
                            </form>
                        </li>
                    <% } %> 
                    </ul>
            </nav>
        </header>
        <!-- <nav class="mobile-nav">
            <ul class="mobile-nav__item-list">
                <li class="mobile-nav__item">
                    <a class="<%= path === '/home' ? 'active' : '' %>" href="/home">Shop</a>
                </li>
                <li class="mobile-nav__item">
                    <a class="<%= path === '/products' ? 'active' : '' %>" href="/products">Products</a>
                </li>
                <% if (isAuthenticated) { %>
                    <li class="mobile-nav__item">
                        <a class="<%= path === '/cart' ? 'active' : '' %>" href="/cart">Cart</a>
                    </li>
                    <li class="mobile-nav__item">
                        <a class="<%= path === '/orders' ? 'active' : '' %>" href="/orders">Orders</a>
                    </li>
                    <li class="mobile-nav__item">
                        <a class="<%= path === '/add-product' ? 'active' : '' %>" href="/add-product">Add Product
                        </a>
                    </li>
                    <li class="mobile-nav__item">
                        <a class="<%= path === '/admin-products' ? 'active' : '' %>" href="/admin-products">Admin Products
                        </a>
                    </li>
                <% } %>
                <% if (!isAuthenticated) { %>
                    <li class="mobile-nav__item">
                        <a class="<%= path === '/login' ? 'active' : '' %>" href="/login">Login</a>
                    </li>
                    <li class="mobile-nav__item">
                        <a class="<%= path === '/signup' ? 'active' : '' %>" href="/signup">Signup</a>
                    </li>
                <% } else { %>
                    <li class="mobile-nav__item">
                        <form action="/logout" method="post">
                            <button type="submit">Logout</button>
                        </form>
                    </li>
                <% } %>
            </ul>
        </nav> -->